#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
source "$ROOT/bin/_lib.sh"

verify_ha_infra_backup

exec "$ROOT/bin/ansible-playbook" \
  -e "ha_infra_backup=$HA_INFRA_BACKUP" \
  playbooks/edge-stack-backup.yml "$@"
